<%- include('nav bar/navbar.ejs') %>
     <link rel="icon" type="image/x-icon" href="/images/TT-removebg-preview.ico">
     <title>Dashboard</title>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <link rel='stylesheet' href='/stylesheets/receipt.css' />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    
    
    <head> 
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    </head>
    
    <main class="table">
        <div class="form-conntainer" style=" height: 650px;   overflow-y: auto;">

          <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<style>
 .client-info-title {
    color: #000000; /* Set your preferred text color */
    font-size: 20px; /* Set your preferred font size */
    margin-bottom: 10px; /* Adjust margin as needed */
    margin: 0 auto; /* Center the element horizontally by setting left and right margins to auto */
    text-align: center;
}
.fa-cash-register {
    color: #378937;
    font-size: 28px;
    margin: 10px;
}
.fa-sack-dollar {
    color: #378937;
    font-size: 28px;
    margin: 10px;
}
.flee
{
display:flex;
}
#datetimee {
        /* Add your desired styles here */
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        font-size: 14px;
        margin: 5px;
    }
     #amounttr , #comment{
        /* Add your desired styles here */
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        font-size: 14px;
        margin: 5px;
        width:300px;
    }

    #floatingSelect {
        /* Add your desired styles here */
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        font-size: 18px;
        margin: 5px;
        margin-left: 15px;
    }
    h1 {
        /* Adjust the font size */
        font-size: 16px;
    }
    /* General styles for the button */
.button {
    display: inline-block;
    padding: 10px 20px;
    font-size: 16px;
    color: #fff;
    background-color: #007BFF; /* Blue background */
    border: none;
    border-radius: 5px;
    text-decoration: none;
    text-align: center;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

/* Hover effect */
.button:hover {
    background-color: #2700b3; /* Darker blue on hover */
}

/* Active effect */
.button:active {
    background-color: #003f7f; /* Even darker blue on active */
}

/* Optional: Additional styles for a more button-like appearance */
.button:focus {
    outline: none; /* Remove default focus outline */
    box-shadow: 0 0 0 4px rgba(0, 123, 255, 0.25); /* Add a focus ring */
}

</style>
<style>
  .dashboard-title {
    font-size: 28px; /* Adjust Size */
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 2px;
    background: linear-gradient(135deg, #1c32fb, #ff1919); /* Gradient Effect */
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: inline-block;
    position: relative;
    padding: 10px 20px;
  }

  .dashboard-title::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 50%;
    height: 3px;
    background: #5563DE;
    border-radius: 5px;
    transform: translateX(-50%);
    transition: width 0.3s ease-in-out;
  }

  .dashboard-title:hover::after {
    width: 100%; /* Expands underline */
  }

  .dashboard-title.glow {
    text-shadow: 0 0 10px rgba(255, 107, 107, 0.8), 0 0 20px rgba(85, 99, 222, 0.8);
    animation: glowEffect 1.5s infinite alternate;
  }

  @keyframes glowEffect {
    0% { text-shadow: 0 0 10px rgba(255, 107, 107, 0.5), 0 0 20px rgba(85, 99, 222, 0.5); }
    100% { text-shadow: 0 0 20px rgba(255, 107, 107, 0.9), 0 0 30px rgba(85, 99, 222, 0.9); }
  }
</style>

            <section class="table__header">
                <h1><strong class="dashboard-title glow">DASHBOARD</strong><button class="btn-scroll" onclick="scrollToSale()">Cash Sale </button></h1> <DIV><STRONG>HI!!!</STRONG>  <%= user.fullname %>  
                </DIV>     
            </section>
            <section class="table__body">
                <div class="flee">
                  <div class="container2">
                    
                    <h1 class="client-info-title">Money Information<a href="/totalmoneydate"><i class="fa-solid fa-eye"></i></a></h1>
                    <div class="card">
                      <div class="card-head">
                        <div class="product-detail">


                          <% let totalAmount = 0; %>

                            <% receiptEdit.forEach(item => { %>
                              <% if (item.inandout === 1) { %>
                                <% totalAmount += item.amount; %>
                              <% } else if (item.inandout === 0) { %>
                                <% totalAmount -= item.amount; %>
                              <% } %>
                            <% }); %>
                         
                          
                          
                          
                            <h1 style="color: #000000; font-family: 'Helvetica Neue', sans-serif; text-align: center; margin-top: 20px; padding: 10px; background-color: #ecf0f1; border-radius: 5px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); display: flex; justify-content: center; align-items: center; position: relative;">

                              <% if (daysAgo >= -1) { %> <!-- Show the left arrow only if daysAgo is greater than 0 -->
                                <a href="/ttdashboard?daysAgo=<%= daysAgo + 1 %>" style="position: absolute; left: 10px; border: 2px solid #ccc; padding: 5px 10px; border-radius: 5px; transition: background-color 0.3s;">
                                  <i class="fa-solid fa-arrow-left"></i>
                                </a>
                              <% } %>
                              
                              <!-- Make the Total Amount clickable to go to today's date (daysAgo = 0) -->
                              <a href="/ttdashboard?daysAgo=0" class="blur-on-hover" style="text-decoration: none; color: #000000; font-family: 'Helvetica Neue', sans-serif;">
                                Total Amount: <%= totalAmount %>
                              </a>
<style>
  .blur-on-hover {
  filter: blur(3px);
  transition: filter 0.3s ease;
}

.blur-on-hover:hover {
  filter: blur(0);
}

</style>                              
                              
                              <% if (daysAgo >= 0) { %> <!-- Show the right arrow only if daysAgo is greater than 0 -->
                                <a href="/ttdashboard?daysAgo=<%= daysAgo - 1 %>" style="position: absolute; right: 10px; border: 2px solid #ccc; padding: 5px 10px; border-radius: 5px; transition: background-color 0.3s;">
                                  <i class="fa-solid fa-arrow-right"></i>
                                </a>
                              <% } %>
                            
                            </h1>
                            
                            <!-- CSS for hover effect on arrows only -->
                            <style>
                              a:not([href="/ttdashboard?daysAgo=0"]):hover {
                                background-color: #4e4e4e;
                                color: white;
                              }
                            </style>
                            
                            
                            
                            

                           
                          </div>
                          <section class="table__body">
                           
                            
                            <table>
                              <thead>
                                <tr>
                                  <th> # </th>
                                  <th> IN</th>
                                  <th> OUT </th>
                                  <th> Detail </th>
                                  <th> Date </th>
                                  <th> Action </th>
                                </tr>
                              </thead>
                            
                              <tbody>
                                <% let totalIn = 0, totalOut = 0, serialNumber = 1; %>
                                <% receiptEdit.reverse().forEach(item => { %>
                                  <% if(item.inandout == 1) totalIn += item.amount; %>
                                  <% if(item.inandout == 0) totalOut += item.amount; %>
                                  <tr id="row-<%= item._id %>">
                                    <td><%= serialNumber++ %></td>
                                    <td class="amount"><% if(item.inandout == 1) { %><%= item.amount %><% } %></td>
                                    <td><% if(item.inandout == 0) { %><%= item.amount %><% } %></td>
                                    <td class="comment"><%= item.comment %> <% if(item.modeofpayment !== 'cash') { %>(<%= item.modeofpayment %>)<% } %></td>
                                    <td>
                                      
                                      <%= new Date(item.Dateandtimeinandout).toUTCString() %>
                                    </td>
                                    
                                    
                                    <td>
                                      <a href="javascript:void(0)" onclick="openEditModal('<%= item._id %>', '<%= item.amount %>', '<%= item.comment %>', '<%= item.modeofpayment %>')"><i class="fa-solid fa-pen-to-square"></i></a>
                                      <a href="#" onclick="showConfirmation('<%= item._id %>')"><i class="fa-solid fa-trash-can"></i></a>
                                    </td>
                                  </tr>
                                <% }); %>
                                <script>
                                  function showConfirmation(itemId) {
                                    if (confirm('Are you sure you want to delete this item?')) {
                                      window.location.href = `/deletemoney/${itemId}`;
                                    }
                                  }
                                </script>
                                <tr>
                                  <td>Total</td>
                                  <td><%= totalIn %></td>
                                  <td><%= totalOut %></td>
                                  
                                </tr>
                              </tbody>
                            </table>
                          </section>
                          
                          <!-- Modal Edit -->
                          <div id="editModal" class="modal">
                            <div class="modal-content">
                              <span class="close">&times;</span>
                              <form id="editForm">
                                <input type="hidden" id="editId">
                                <label for="editAmount">Amount:</label>
                                <input type="number" id="editAmount" name="amount" step="0.01">
                                <label for="editComment">Comment:</label>
                                <input type="text" id="editComment" name="comment">
                                <label for="editModeOfPayment">Mode of Payment:</label>
                                <select id="editModeOfPayment" name="modeofpayment" class="styled-select">
                                    <option value="cash">Cash</option>
                                    <option value="paytm">Paytm</option>
                                </select>
                                <button type="button" onclick="submitEditForm()">Save</button>
                              </form>
                            </div>
                          </div>
                          
                          <style>
                            .modal { display: none; position: fixed; z-index: 1; left: 0; top: 0; width: 100%; height: 100%; overflow: auto; background-color: rgba(0,0,0,0.4); }
                            .modal-content { background-color: #fff; margin: 15% auto; padding: 20px; border: 1px solid #888; width: 80%; }
                            .close { color: #aaa; float: right; font-size: 28px; font-weight: bold; cursor: pointer; }
                            .close:hover { color: black; }
                            #editForm { display: flex; flex-direction: column; }
                            #editForm input, select { padding: 8px; margin-bottom: 10px; border: 1px solid #ccc; border-radius: 4px; }
                            #editForm button { padding: 10px; background-color: #4CAF50; color: white; border: none; border-radius: 4px; cursor: pointer; }
                            .total-link { font-weight: bold; text-decoration: none; color: blue; }
                            .total-link:hover { text-decoration: underline; }
                          </style>
                          
                          <script>
                            var modal = document.getElementById("editModal");
                            var span = document.getElementsByClassName("close")[0];
                            span.onclick = function() { modal.style.display = "none"; }
                            window.onclick = function(event) { if (event.target == modal) modal.style.display = "none"; }
                          
                            function openEditModal(id, amount, comment, modeOfPayment) {
                              document.getElementById("editId").value = id;
                              document.getElementById("editAmount").value = amount;
                              document.getElementById("editComment").value = comment;
                              document.getElementById("editModeOfPayment").value = modeOfPayment;
                              modal.style.display = "block";
                            }
                          
                            function submitEditForm() {
                              var id = document.getElementById("editId").value;
                              var amount = document.getElementById("editAmount").value;
                              var comment = document.getElementById("editComment").value;
                              var modeofpayment = document.getElementById("editModeOfPayment").value;
                          
                              fetch("/editmoney/" + id, {
                                method: "POST",
                                headers: { "Content-Type": "application/json" },
                                body: JSON.stringify({ amount: amount, comment: comment, modeofpayment: modeofpayment })
                              })
                              .then(response => response.json())
                              .then(response => {
                                var row = document.getElementById("row-" + id);
                                row.querySelector(".amount").innerText = response.amount;
                                row.querySelector(".comment").innerText = response.comment + (response.modeofpayment === 'paytm' ? " (Paytm)" : "");
                                modal.style.display = "none";
                              })
                              .catch(error => alert("Error: " + error));
                            }
                          </script>
                          
                          
                          <!-- Modal Edit -->
                          <div id="editModal" class="modal">
                            <div class="modal-content">
                              <span class="close">&times;</span>
                              <form id="editForm">
                                <input type="hidden" id="editId">
                                <label for="editAmount">Amount:</label>
                                <input type="number" id="editAmount" name="amount" step="0.01">
                                <label for="editComment">Comment:</label>
                                <input type="text" id="editComment" name="comment">
                                <label for="editModeOfPayment">Mode of Payment:</label>
                                <select id="editModeOfPayment" name="modeofpayment" class="styled-select">
                                    <option value="cash">Cash</option>
                                    <option value="paytm">Paytm</option>
                                </select>
                                <button type="button" onclick="submitEditForm()">Save</button>
                              </form>
                            </div>
                          </div>
                          
                          <style>
                            .modal { display: none; position: fixed; z-index: 1; left: 0; top: 0; width: 100%; height: 100%; overflow: auto; background-color: rgba(0,0,0,0.4); }
                            .modal-content { background-color: #fff; margin: 15% auto; padding: 20px; border: 1px solid #888; width: 80%; }
                            .close { color: #aaa; float: right; font-size: 28px; font-weight: bold; cursor: pointer; }
                            .close:hover { color: black; }
                            #editForm { display: flex; flex-direction: column; }
                            #editForm input, select { padding: 8px; margin-bottom: 10px; border: 1px solid #ccc; border-radius: 4px; }
                            #editForm button { padding: 10px; background-color: #4CAF50; color: white; border: none; border-radius: 4px; cursor: pointer; }
                            .total-link { font-weight: bold; text-decoration: none; color: blue; }
                            .total-link:hover { text-decoration: underline; }
                          </style>
                          
                          <script>
                            var modal = document.getElementById("editModal");
                            var span = document.getElementsByClassName("close")[0];
                            span.onclick = function() { modal.style.display = "none"; }
                            window.onclick = function(event) { if (event.target == modal) modal.style.display = "none"; }
                          
                            function openEditModal(id, amount, comment, modeOfPayment) {
                              document.getElementById("editId").value = id;
                              document.getElementById("editAmount").value = amount;
                              document.getElementById("editComment").value = comment;
                              document.getElementById("editModeOfPayment").value = modeOfPayment;
                              modal.style.display = "block";
                            }
                          
                            function submitEditForm() {
                              var id = document.getElementById("editId").value;
                              var amount = document.getElementById("editAmount").value;
                              var comment = document.getElementById("editComment").value;
                              var modeofpayment = document.getElementById("editModeOfPayment").value;
                          
                              fetch("/editmoney/" + id, {
                                method: "POST",
                                headers: { "Content-Type": "application/json" },
                                body: JSON.stringify({ amount: amount, comment: comment, modeofpayment: modeofpayment })
                              })
                              .then(response => response.json())
                              .then(response => {
                                var row = document.getElementById("row-" + id);
                                row.querySelector(".amount").innerText = response.amount;
                                row.querySelector(".comment").innerText = response.comment + (response.modeofpayment === 'paytm' ? " (Paytm)" : "");
                                modal.style.display = "none";
                              })
                              .catch(error => alert("Error: " + error));
                            }
                          </script>
                        </div>
                        
                      </div>
                    </div>
                  <div class="container">
                    <h1 class="client-info-title">Add Transaction</h1>
                    <div class="card">
                      <div class="card-head">
                    
                        <div class="product-detail">
                            
                          <form action="/addtransactiondashboard" method="POST">
                          <div class="flee">
                            <div>
                              <h1><label for="datetimee">Date and Time:</label></h1>
                              <input type="datetime-local" id="datetimee" name="datetimee" required>
                          </div>
                          <div>
                              <h1><label >Works with selects</label></h1>
                                  <select name="itemCategory" id="floatingSelect" >
                                      <option  value="1" selected>Credit</option>
                                      <option value="0">Debit</option>                                     
                                  </select>
                         </div>
                              </div>
                             
                              <h1><label for="amounttr">Amount:</label></h1>
                              <input type="number" id="amounttr" required name="amounttr">

                              <h1><label >Mode of payment :</label></h1>
                              <select name="modeofpayment" id="modeofpayment" class="styled-select">
                                <option value="cash" selected>Cash</option>
                                <option value="paytm">Paytm</option>
                            </select>
<style>
  .styled-select {
    width: 200px;                /* Adjust the width of the select box */
    padding: 10px;               /* Add padding inside the select box */
    border: 1px solid #ccc;      /* Border around the select box */
    border-radius: 5px;          /* Rounded corners */
    background-color: #f7f7f7;   /* Light grey background */
    font-size: 16px;             /* Font size */
    color: #333;                 /* Font color */
    appearance: none;            /* Remove default arrow */
    -webkit-appearance: none;    /* Remove default arrow in Safari */
    -moz-appearance: none;       /* Remove default arrow in Firefox */
    background-image: url('data:image/svg+xml;base64,PHN2ZyB...'); /* Custom arrow icon */
    background-repeat: no-repeat;
    background-position: right 10px center;
    background-size: 12px;       /* Size of the custom arrow icon */
}

.styled-select:focus {
    border-color: #66afe9;       /* Border color on focus */
    box-shadow: 0 0 5px rgba(102, 175, 233, 0.5); /* Shadow on focus */
    outline: none;               /* Remove default outline */
}

.styled-select option {
    padding: 10px;               /* Padding inside the options */
    background-color: #ffffff;   /* Background color of options */
    color: #333;                 /* Font color of options */
}

</style>                            

                             

                              <h1><label for="comment">comment:</label></h1>
                              <input type="text" id="comment" name="comment">
                              
                              <input type="submit" value="Submit" class="submit-button">

                            </div>
                           
                          
                 
                         


                                                    
                          

                          </div>
                          </div>
                        </form>
                        
                      </div>
                      
                      
                      
                      
                    </div>
                    <% 
                    let totalCashIn = 0;
                    let totalCashOut = 0;
                    let totalPaytmIn = 0;
                    let totalPaytmOut = 0;
                
                    console.log('Receipt Edit:', JSON.stringify(receiptEdit, null, 2)); // Debugging the receiptEdit data
                
                    receiptEdit.forEach(item => {
                        if (item.modeofpayment != 'paytm') {
                            if (item.inandout == 1) {
                                totalCashIn += item.amount;
                            } else if (item.inandout == 0) {
                                totalCashOut += item.amount;
                            }
                        } else if (item.modeofpayment == 'paytm') {
                            if (item.inandout == 1) {
                                totalPaytmIn += item.amount;
                            } else if (item.inandout == 0) {
                                totalPaytmOut += item.amount;
                            }
                        }
                    });
                
                    // Calculate the net totals
                    totalCashIn = totalCashIn - totalCashOut;
                    totalPaytmIn = totalPaytmIn - totalPaytmOut;
                
                    console.log('Net Total Cash In:', totalCashIn); // Debugging the net total amounts
                    console.log('Net Total Paytm In:', totalPaytmIn);
                %>
                
                
                  
                 <!-- Set the width and height using inline styles or a CSS class -->
                 <canvas id="paymentChart" style="width: 5px; height: 5px;"></canvas>

                 <script>
                   var ctx = document.getElementById('paymentChart').getContext('2d');
                   var paymentChart = new Chart(ctx, {
                     type: 'pie',
                     data: {
                       labels: ['Cash', 'Paytm'],
                       datasets: [{
                         label: 'Total In',
                         data: [<%= totalCashIn %> , <%= totalPaytmIn %>],
                         backgroundColor: ['#A2C2E2', '#B6E2B6'], // Light Blue and Light Green
                         borderColor: ['#7D9AC4', '#9BCC9C'], 
                         borderWidth: 1
                       }]
                     },
                     options: {
                       responsive: true,
                       maintainAspectRatio: true,
                       plugins: {
                         legend: {
                           position: 'top',
                         },
                         tooltip: {
                           callbacks: {
                             label: function(context) {
                               return context.label + ': Rs ' + context.raw;
                             }
                           }
                         }
                       }
                     }
                   });
                 </script>


                      <hr style="border: 1px solid #c9c8c8; margin: 30px 0;">

    </section>
    <div class="tablee"  id="saleEntry">
      <section class="table__body">
     <a href="/saleall" style="text-decoration: none; color: inherit;">
  <div class="card-headerr">
    <i class="fas fa-shopping-cart"></i> Sale Entry
  </div>
</a>

      <div class="card-body">
        <form action="/sale" method="POST">
          <div class="row mb-4">
            <div class="flex flex-wrap p-6 rounded-md">
                <div class="flex flex-wrap -mx-2 mb-4">
        
                    <!-- Date and Time Field -->
                    <div class="w-full md:w-1/4 px-2 mb-4 md:mb-0 relative">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="datetimereceipt">Date and Time</label>
                        <input id="datetimereceipt" name="datetimereceipt" class="form-input w-full py-2 px-3 border border-gray-800 rounded-md focus:outline-none focus:border-blue-500" type="datetime-local">
                    </div>
        
                    <!-- Name Field -->
                    <div class="w-full md:w-1/4 px-3 mb-6 md:mb-0 relative">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="inputusername">Name</label>
                        <div>
                          <input id="inputusername" name="Name" placeholder="Name" class="form-input w-full py-2 px-3 border border-gray-800 rounded-md focus:outline-none focus:border-blue-500" type="text">

                          <div id="autocompleteDropdown" class="autocomplete-dropdown"></div>
                        </div>
                    </div>
        
                    <!-- Address Field -->
                    <div class="w-full md:w-1/4 px-3">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="inputAddress">Address</label>
                        <input id="inputAddress" name="Address" placeholder="Address" class="form-input w-full py-2 px-3 border border-gray-800 rounded-md focus:outline-none focus:border-blue-500" type="text">
                    </div>
        
                    <!-- Phone Field with Improved Validation -->
                    <div class="w-full md:w-1/4 px-3">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="inputPhone">Phone</label>
                        <input id="inputPhone" name="Phone" placeholder="Phone" class="form-input w-full py-2 px-3 border border-gray-800 rounded-md focus:outline-none focus:border-blue-500" type="text">
                        <p id="phoneError" class="text-red-500 text-xs mt-1 hidden">Phone number must be exactly 10 digits.</p>
                    </div>
        
                    <!-- Comment Field -->
                   
        
                </div>
            </div>
        </div>
        <script>
              function handleAutocomplete(autocompleteInput, row) {
    const autocompleteDropdown = row.querySelector('.autocomplete-dropdown');
    const rentInput = row.querySelector('input[name="rent[]"]');
    const amountInput = row.querySelector('input[name="amount[]"]');
    const quantityInput = row.querySelector('input[name="quantity[]"]');
  
    const inputValue = autocompleteInput.value.trim();
    if (inputValue === "") {
      autocompleteDropdown.innerHTML = "";
      rentInput.value = ""; // Clear rent if item name is empty
      amountInput.value = ""; // Clear amount if item name is empty
      return;
    }
  
    const timestamp = new Date().getTime();
    const url = `/itemname/${inputValue}?_=${timestamp}`;
  
    axios.get(url)
      .then(function (response) {
        const data = response.data;
        const autocompleteItems = data.map(item => `
          <div class="autocomplete-item">
            <span class="autocomplete-name">${item.itemName}</span>
            <span class="autocomplete-address">${item.totalQuantity ? `(${item.workingQuantity} in stock)` : '(No stock)'}</span>
            </div>`
        ).join("");
  
        autocompleteDropdown.innerHTML = autocompleteItems;
  
        const autocompleteItemElements = document.querySelectorAll(".autocomplete-item");
        autocompleteItemElements.forEach(itemElement => {
          itemElement.addEventListener("click", function () {
            const selecteditemName = itemElement.querySelector('.autocomplete-name').innerText;
            const selecteditem = data.find(client => client.itemName === selecteditemName);
  
            if (selecteditem) {
              autocompleteInput.value = selecteditem.itemName + (selecteditem.workingQuantity ? ` (${selecteditem.workingQuantity} in stock)` : '');
              rentInput.value = selecteditem.rentPrice;
              if (selecteditem.itemCategory == 1) {
      // Assuming you want to set quantity to 1 if itemCategory is 1
      quantityInput.value = 1;
    }
              amountInput.value = calculateAmount(row);
              autocompleteDropdown.innerHTML = "";
            } else {
              console.warn("Item not found for selected name:", selecteditemName);
            }
          });
        });
      })
      .catch(function (error) {
        console.error("Error fetching data:", error);
    });
  }
           document.addEventListener('DOMContentLoaded', function () {
    // Get the current date and time in the format expected by datetime-local input
    function getCurrentDateTime() {
        const now = new Date();
        const year = now.getFullYear();
        const month = String(now.getMonth() + 1).padStart(2, '0');
        const day = String(now.getDate()).padStart(2, '0');
        const hours = String(now.getHours()).padStart(2, '0');
        const minutes = String(now.getMinutes()).padStart(2, '0');
    
        return `${year}-${month}-${day}T${hours}:${minutes}`;
    }
    
    // Set the current date and time to the input field
    const datetimeInput = document.getElementById('datetimereceipt');
    datetimeInput.value = getCurrentDateTime();
    });
    
    
        </script>
        <script>

    document.addEventListener("DOMContentLoaded", function () {
      const inp = document.querySelector("#inputusername");
      console.log(inp);
      const dropdown = document.getElementById("autocompleteDropdown");
      const inputAddress = document.getElementById("inputAddress");
      const inputPhone = document.getElementById("inputPhone");
      const inputcomment = document.getElementById("inputcomment");
  let selectedUserId = null;
      inp.addEventListener("input", function () {
        const inputValue = inp.value.trim();
        if (inputValue === "") {
          dropdown.innerHTML = "";
          inputAddress.value = "";
          inputPhone.value = "";
          inputcomment.value = "";
          return;
        }

        const timestamp = new Date().getTime();
        const url = `/username/${inputValue}?_=${timestamp}`;

        axios.get(url)
          .then(function (response) {
            const data = response.data;
            console.log(data);
            const autocompleteItems = data.map(item => `
              <div class="autocomplete-item">
                <span class="autocomplete-name">${item.clientName}</span>
                <span class="autocomplete-address">${item.address || 'No address'}</span>
              </div>`
            ).join("");

            dropdown.innerHTML = autocompleteItems;

            const autocompleteItemElements = document.querySelectorAll(".autocomplete-item");
            autocompleteItemElements.forEach(item => {
              item.addEventListener("click", function () {
                const selectedClientName = item.querySelector('.autocomplete-name').innerText;
            const selectedClientAddress = item.querySelector('.autocomplete-address').innerText;
            const selectedClient = data.find(client => client.clientName === selectedClientName && client.address === selectedClientAddress);
  console.log('aa');
  if (selectedClient) {
    inp.value = selectedClient.clientName;
    inputAddress.value = selectedClient.address || "";
    inputPhone.value = selectedClient.phone || "";
    inputcomment.value = selectedClient.comment || "";
     selectedUserId = selectedClient._id; // Store the selected user's ID
    console.log("Updated values:", inputAddress.value, inputPhone.value, inputcomment.value, selectedUserId);
    dropdown.innerHTML = "";

    const count = selectedClient.receiptinit ? selectedClient.receiptinit.length : 0;
              receiptInitCountBox.innerText = count;
  } else {
    console.warn("Client not found for selected name:", selectedClientName);
}
});

            });
          })
          .catch(function (error) {
            console.error("Error fetching data:", error);
          });
       receiptInitCountBox.addEventListener("click", function () {
        if (selectedUserId) {
  window.open(`/lastallaccount/${selectedUserId}`, '_blank');
} else {
  alert("Please select a user first.");
}

  });
});

      inp.addEventListener("keydown", function (event) {
        const autocompleteItemElements = document.querySelectorAll(".autocomplete-item");
        if (event.key === "ArrowDown" && autocompleteItemElements.length > 0) {
          autocompleteItemElements[0].focus();
        } else if (event.key === "ArrowUp" && autocompleteItemElements.length > 0) {
            autocompleteItemElements[autocompleteItemElements.length - 1].focus();
        }
      });

      document.addEventListener("click", function (event) {
        if (!inp.contains(event.target) && !dropdown.contains(event.target)) {
          dropdown.innerHTML = "";
        }
      });
    });



</script>

         <style>
    
         
          
          
          
          .autocomplete-dropdown {
          position: absolute;
          width: 200px; /* Set a fixed width for the dropdown */
          max-height: 150px;
          overflow-y: auto;
          border: 1px solid #dddddd; /* Match the border style of table cells */
          background-color: #ffffff; /* Set the background color to white */
          z-index: 1; /* Ensure the dropdown appears above other elements */
          }
          
          
          
          .autocomplete-dropdownn {
          position: absolute;
          width: 200px; /* Set a fixed width for the dropdown */
          max-height: 150px;
          overflow-y: auto;
          border: 1px solid #dddddd; /* Match the border style of table cells */
          background-color: #ffffff; /* Set the background color to white */
          z-index: 1; /* Ensure the dropdown appears above other elements */
          }
          
          .form-row {
          border: 1px solid #ddd;
          border-radius: 8px;
          margin-bottom: 15px;
          overflow: hidden;
          background-color: #f5f5f5;
          transition: box-shadow 0.3s ease;
          width: 110%;
          margin: 20px;
          }
          
          /* Style the serial number column */
          .serial-number {
          font-weight: bold;
          padding: 10px;
          border-right: 1px solid #ddd;
          background-color: #e0e0e0;
          }
          
         
          
          /* Style the autocomplete dropdown */
          .autocomplete-dropdown {
          width: 100%;
          }
          
          .autocomplete-dropdownn{
          width: 100%;
          }
          
          
          
          /* Style the delete button */
          
          
         
          
          /* Style the table row on hover for better UX */
          .form-row:hover {
          box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
          }
          
          
          
          
          
          
          /* Center the submit button */
          
          
          
          
    
       
      
      
      
      
      
      
      
      
      
      
     
              #toggle-form-button:hover {
                background-color: #2980b9;
              }
          
          
          
              button {
              border: none;
              outline: none;
              cursor: pointer;
          }
          
        
          
       
          
          
        
          
          
       
          
         
              hr {
              border: 2px solid #a09d9d; /* Set the border color and style */
              margin: 20px 0; /* Set margin for spacing */
            } 
          
              .stock-label {
                  font-size: 15px;
                  color: #333; /* Dark gray color */
                  margin-bottom: 5px;
              }
          
              .stock-number {
                  display: inline-block;
                  padding: 5px 10px;
                  color: #000000; /* Black text color */
                  font-weight: bold;
                  border-radius: 5px;
                  border: 2px solid #2E8B57; /* Darker green border */
              }
          </style>
        <!-- JavaScript for Improved Phone Validation -->
        <script>
        document.addEventListener('DOMContentLoaded', function() {
            const phoneInput = document.getElementById('inputPhone');
            const phoneError = document.getElementById('phoneError');
        
            phoneInput.addEventListener('input', function() {
                const phoneValue = phoneInput.value.trim();
        
                // Allow only numbers and enforce exactly 10 digits
                if (!/^\d{10}$/.test(phoneValue)) {
                    phoneError.classList.remove('hidden');
                } else {
                    phoneError.classList.add('hidden');
                }
            });
        });
        </script>
        

        <h4 class="mb-3 text-xl font-bold uppercase tracking-wide relative items-sold-title">
          <i class="fas fa-box-open mr-2 text-blue-500"></i> Items Sold
      </h4> <button type="button" class="add-row-button mt-3 p-2 bg-blue-500 text-white rounded-md hover:bg-blue-600" onclick="addRow()">
        ➕ Add Item Row
      </button>
      
      <style>
      .items-sold-title {
          color: #5563DE; /* Your original color */
          font-family: 'Montserrat', sans-serif;
          padding-bottom: 5px;
          border-bottom: 3px solid #74ABE2;
          display: inline-block;
          position: relative;
      }
      
      .items-sold-title::after {
          content: "";
          width: 40px;
          height: 3px;
          background-color: #74ABE2;
          position: absolute;
          bottom: -6px;
          left: 50%;
          transform: translateX(-50%);
          border-radius: 2px;
      }
      </style>
  
  <div >
    <table class="w-full border-collapse border border-gray-300 shadow-lg rounded-md">
      <thead class="bg-blue-500 text-white">
        <tr>
          <th class="p-3 border">#</th>
          <th class="p-3 border">Item Name</th>
          <th class="p-3 border">Quantity</th>
          <th class="p-3 border">Rent</th>
          <th class="p-3 border">Amount</th>
          <th class="p-3 border">Action</th>
        </tr>
      </thead>
  
        <tbody id="form-rows">
          <tr class="form-row">
            <td class="serial-number">1</td>
            <td hidden><input type="datetime-local" name="datetime[]" hidden></td>
            <td class="relative">
                <input type="text" name="item[]" placeholder="SELECT ITEM" required class="input-field">
                <div class="autocomplete-dropdown"></div>
            </td>
            <style>
              
            </style>
            <td><input type="text" name="quantity[]" required placeholder="Quantity" class="input-field"></td>
            <td><input type="text" name="rent[]" placeholder="RATE" class="input-field"></td>
            <td><input type="text" name="amount[]" placeholder="TOTAL" class="input-field"></td> 
            <td>
                <button type="button" class="delete-button" onclick="deleteRow(this)">
                    <i class="fa-solid fa-trash-can"></i>
                </button>
            </td>
        </tr>
        
      
        
        
        </tbody>
  
      <!-- Additional Charges Section -->
      <tbody id="additional-charges-section" class="bg-gray-50">
        <tr>
          <td colspan="3" class="border"></td>
          <td class="p-3 border">
            <input type="text" name="Additionalchargesname[]" placeholder="Enter Additional Charges Name" class="input-field">
          </td>
          <td class="p-3 border">
            <input type="text" name="AdditionalchargesAmount[]" placeholder="Additional Charges Amount" class="input-field">
          </td>
          <td class="p-3 border text-center">
            <button type="button" class="add-additional text-blue-500 hover:text-blue-700" onclick="addAdditionalChargeRow(this)">
              <i class="fa-solid fa-plus"></i>
            </button>
          </td>
        </tr>
      </tbody>
      
      <script>
        function addAdditionalChargeRow(button) {
          const tbody = document.getElementById('additional-charges-section');
      
          // Create a new row element
          const newRow = document.createElement('tr');
          newRow.innerHTML = `
            <td colspan="3" class="border"></td>
            <td class="p-3 border">
              <input type="text" name="Additionalchargesname[]" placeholder="Enter Additional Charges Name" class="input-field">
            </td>
            <td class="p-3 border">
              <input type="text" name="AdditionalchargesAmount[]" placeholder="Additional Charges Amount" class="input-field">
            </td>
            <td class="p-3 border text-center">
              <button type="button" class="remove-additional text-red-500 hover:text-red-700" onclick="removeAdditionalChargeRow(this)">
                <i class="fa-solid fa-minus"></i>
              </button>
            </td>
          `;
          tbody.appendChild(newRow);
        }
      
        function removeAdditionalChargeRow(button) {
          const row = button.closest('tr');
          row.remove();
        }
      </script>
      <!-- Grand Total Row -->
<tfoot>
  <tr class="bg-green-100 font-bold">
    <td colspan="4" class="text-right p-3 border">Grand Total:</td>
    <td class="p-3 border" id="grand-total">Rs 0</td>
    <td class="border"></td>
  </tr>
</tfoot>

    </table>
  
   
  </div>
  <script>
    document.addEventListener('input', function(event) {
      const name = event.target.name;
  
      if (name === 'quantity[]' || name === 'rent[]') {
        const row = event.target.closest('tr');
        const quantity = parseFloat(row.querySelector('input[name="quantity[]"]').value) || 0;
        const rent = parseFloat(row.querySelector('input[name="rent[]"]').value) || 0;
        const amountField = row.querySelector('input[name="amount[]"]');
        if (amountField) {
          amountField.value = (quantity * rent).toFixed(2);
          updateGrandTotal();  // ✅ FIX is here
        }
      }
  
      if (name === 'amount[]' || name === 'AdditionalchargesAmount[]') {
        updateGrandTotal(); // ✅ Also update if user edits manually
      }
    });
  </script>
  
  <script>
    function updateGrandTotal() {
      let total = 0;
  
      // Calculate total from main item rows
      document.querySelectorAll('input[name="amount[]"]').forEach(input => {
        total += parseFloat(input.value) || 0;
      });
  
      // Calculate total from additional charges
      document.querySelectorAll('input[name="AdditionalchargesAmount[]"]').forEach(input => {
        total += parseFloat(input.value) || 0;
      });
  
      // Show total in some element (e.g., span or div with id="grand-total")
      const totalEl = document.getElementById('grand-total');
      if (totalEl) totalEl.innerText = `Rs ${total.toFixed(2)}`;
    }
  </script>
  
  
  
 
  
  <style>
    .input-field {
      width: 100%;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
      outline: none;
      transition: border 0.3s ease-in-out;
    }
  
    .input-field:focus {
      border-color: #5563DE;
      box-shadow: 0 0 5px rgba(85, 99, 222, 0.5);
    }
  
    .delete-button {
      background: transparent;
      border: none;
      font-size: 1.2rem;
      cursor: pointer;
      transition: transform 0.2s;
    }
  
    .delete-button:hover {
      transform: scale(1.1);
    }
  
    .add-row-button {
      display: inline-block;
      padding: 10px 15px;
      border-radius: 5px;
      font-size: 1rem;
    }
  </style>
   
  <style>
    /* Style for table elements */
    .input-field {
      width: 100%;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
      outline: none;
      transition: border 0.3s ease-in-out;
    }
  
    .input-field:focus {
      border-color: #5563DE;
      box-shadow: 0 0 5px rgba(85, 99, 222, 0.5);
    }
  
    .delete-button {
      background: transparent;
      border: none;
      font-size: 1.2rem;
      cursor: pointer;
      transition: transform 0.2s;
    }
  
    .delete-button:hover {
      transform: scale(1.1);
    }
  </style>
   <script>

    let rowCounter = 1; // Counter for unique IDs

    function handleAutocompleteInputEvent(itemInput, row) {
      itemInput.addEventListener("input", function () {
        handleAutocomplete(itemInput, row);
      });
    }

    document.addEventListener("DOMContentLoaded", function () {
      // Set default value for the initial row
      const firstDatetimeInput = document.querySelector('input[name="datetime[]"]');
      setDefaultDatetime(firstDatetimeInput);

      const firstItemInput = document.querySelector('input[name="item[]"]');
      const firstRow = document.querySelector('.form-row');
      handleAutocompleteInputEvent(firstItemInput, firstRow);
    });

    function addRow() {
      const formRows = document.getElementById('form-rows');
      const newRow = document.createElement('tr');
      newRow.className = 'form-row';

      // Get the current date and time
      const currentDate = new Date();
      const year = currentDate.getFullYear();
      const month = String(currentDate.getMonth() + 1).padStart(2, '0');
      const day = String(currentDate.getDate()).padStart(2, '0');
      const hours = String(currentDate.getHours()).padStart(2, '0');
      const minutes = String(currentDate.getMinutes()).padStart(2, '0');

      const currentDatetime = `${year}-${month}-${day}T${hours}:${minutes}`;

      newRow.innerHTML = `
        <td class="serial-number">${++rowCounter}</td>
        <td hidden><input type="datetime-local" name="datetime[]" value="${currentDatetime}" required></td>
        <td>
          <input type="text" name="item[]" placeholder="SELECT ITEM" class="input-field" required>
          <div class="autocomplete-dropdown"></div>
        </td>
        <td><input type="text" name="quantity[]" required placeholder="Quantity" class="input-field"></td>
        <td><input type="text" name="rent[]" placeholder="RATE" class="input-field"></td>
        <td><input type="text" name="amount[]" placeholder="TOTAL" class="input-field"></td>
        <td><button type="button" class="delete-button" onclick="deleteRow(this)"><i class="fa-solid fa-trash-can"></i> </button></td>
      `;
      formRows.appendChild(newRow);

      // Update the new item input value
      const newItemInput = newRow.querySelector('input[name="item[]"]');
      handleAutocompleteInputEvent(newItemInput, newRow);



      const newRowRentInput = newRow.querySelector('input[name="rent[]"]');
  const newRowQuantityInput = newRow.querySelector('input[name="quantity[]"]');

  newRowRentInput.addEventListener("input", function () {
      updateAmount(newRow);
  });

  newRowQuantityInput.addEventListener("input", function () {
      updateAmount(newRow);
  });
    }
    
    function deleteRow(button) {
      const formRows = document.getElementById('form-rows');
      const rows = formRows.getElementsByClassName('form-row');
      const row = button.closest('.form-row');
      const serialNumberCell = row.querySelector('.serial-number');

      // Update the serial numbers after deleting a row
      for (let i = Array.from(rows).indexOf(row) + 1; i < rows.length; i++) {
        const serialNumberCell = rows[i].querySelector('.serial-number');
        serialNumberCell.textContent = i;
      }

      rowCounter--;

      formRows.removeChild(row);
    }

    function handleAutocomplete(autocompleteInput, row) {
  const autocompleteDropdown = row.querySelector('.autocomplete-dropdown');
  const rentInput = row.querySelector('input[name="rent[]"]');
  const amountInput = row.querySelector('input[name="amount[]"]');
  const quantityInput = row.querySelector('input[name="quantity[]"]');

  const inputValue = autocompleteInput.value.trim();
  if (inputValue === "") {
    autocompleteDropdown.innerHTML = "";
    rentInput.value = ""; // Clear rent if item name is empty
    amountInput.value = ""; // Clear amount if item name is empty
    return;
  }

  const timestamp = new Date().getTime();
  const url = `/itemname/${inputValue}?_=${timestamp}`;

  axios.get(url)
    .then(function (response) {
      const data = response.data;
      const autocompleteItems = data.map(item => `
        <div class="autocomplete-item">
          <span class="autocomplete-name">${item.itemName}</span>
          <span class="autocomplete-address">${item.totalQuantity ? `(${item.totalQuantity} in stock)` : '(No stock)'}</span>
        </div>`
      ).join("");

      autocompleteDropdown.innerHTML = autocompleteItems;

      const autocompleteItemElements = document.querySelectorAll(".autocomplete-item");
      autocompleteItemElements.forEach(itemElement => {
        itemElement.addEventListener("click", function () {
          const selecteditemName = itemElement.querySelector('.autocomplete-name').innerText;
          const selecteditem = data.find(client => client.itemName === selecteditemName);

          if (selecteditem) {
            autocompleteInput.value = selecteditem.itemName + (selecteditem.totalQuantity ? ` (${selecteditem.totalQuantity} in stock)` : '');
            rentInput.value = selecteditem.sellingPrice;
            if (selecteditem.itemCategory == 1) {
    // Assuming you want to set quantity to 1 if itemCategory is 1
    quantityInput.value = 1;
  }
            amountInput.value = calculateAmount(row);
            updateGrandTotal();
            autocompleteDropdown.innerHTML = "";
          } else {
            console.warn("Item not found for selected name:", selecteditemName);
          }
        });
      });
    })
    .catch(function (error) {
      console.error("Error fetching data:", error);
    });
}


function calculateAmount(row) {
  const quantity = parseFloat(row.querySelector('input[name="quantity[]"]').value) || 0;
  const rent = parseFloat(row.querySelector('input[name="rent[]"]').value) || 0;
  return (quantity * rent).toFixed(2);
}


    function setDefaultDatetime(datetimeInput) {
      const currentDate = new Date();
      const year = currentDate.getFullYear();
      const month = String(currentDate.getMonth() + 1).padStart(2, '0');
      const day = String(currentDate.getDate()).padStart(2, '0');
      const hours = String(currentDate.getHours()).padStart(2, '0');
      const minutes = String(currentDate.getMinutes()).padStart(2, '0');

      const currentDatetime = `${year}-${month}-${day}T${hours}:${minutes}`;
      datetimeInput.value = currentDatetime;
    }

    document.addEventListener("click", function (event) {
      const autocompleteInputs = document.querySelectorAll('input[name="item[]"]');
      const autocompleteDropdowns = document.querySelectorAll('.autocomplete-dropdown');

      for (let i = 0; i < autocompleteInputs.length; i++) {
        if (!autocompleteInputs[i].contains(event.target) && !autocompleteDropdowns[i].contains(event.target)) {
          autocompleteDropdowns[i].innerHTML = "";
        }
      }
    });

  
    document.addEventListener("DOMContentLoaded", function () {
  
  const rentInputs = document.querySelectorAll('input[name="rent[]"]');
  const quantityInputs = document.querySelectorAll('input[name="quantity[]"]');

  rentInputs.forEach(rentInput => {
      rentInput.addEventListener("input", function () {
          updateAmount(this.closest('.form-row'));
      });
  });

  quantityInputs.forEach(quantityInput => {
      quantityInput.addEventListener("input", function () {
          updateAmount(this.closest('.form-row'));
      });
  });
});
function updateAmount(row) {
  const quantity = parseFloat(row.querySelector('input[name="quantity[]"]').value) || 0;
  const rent = parseFloat(row.querySelector('input[name="rent[]"]').value) || 0;
  const amountInput = row.querySelector('input[name="amount[]"]');
  
  // Update the Amount field
  amountInput.value = (quantity * rent).toFixed(2);
}


$(document).ready(function () {
  $('input, select').keydown(function (e) {
    if (e.which === 38) { // Up arrow key
      e.preventDefault();
      navigateUpDown(this, 'prev');
    } else if (e.which === 40) { // Down arrow key
      e.preventDefault();
      navigateUpDown(this, 'next');
    } else if (e.which === 37) { // Left arrow key
      e.preventDefault();
      navigateLeftRight(this, 'prev');
    } else if (e.which === 39) { // Right arrow key
      e.preventDefault();
      navigateLeftRight(this, 'next');
    }
  });
});

function navigateUpDown(element, direction) {
  const targetElement = $(element)[direction]();
  if (targetElement.length > 0) {
    targetElement.focus();
  }
}

function navigateLeftRight(element, direction) {
  const targetElement = $(element).closest('td')[direction]().find('input, select');
  if (targetElement.length > 0) {
    targetElement.focus();
  }
}





  </script>
      


  <div class="text-center mt-4">
    <button type="submit" class="btn-gradient">
      <i class="fas fa-save"></i> Submit Sale
    </button>
  </div>
  
  <style>
    .btn-gradient {
      background: linear-gradient(135deg, #5563DE, #74ABE2);
      color: white;
      font-size: 1.2rem;
      font-weight: bold;
      padding: 12px 24px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      transition: all 0.3s ease-in-out;
      box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.2);
      text-transform: uppercase;
      letter-spacing: 1px;
      position: relative;
      overflow: hidden;
    }
  
    .btn-gradient::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      width: 300%;
      height: 300%;
      background: radial-gradient(circle, rgba(255,255,255,0.2) 10%, transparent 70%);
      transition: all 0.6s ease-in-out;
      transform: translate(-50%, -50%) scale(0);
    }
  
    .btn-gradient:hover::before {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0;
    }
  
    .btn-gradient:hover {
      transform: translateY(-3px);
      box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.25);
    }
  
    .btn-gradient:active {
      transform: translateY(2px);
      box-shadow: 0px 3px 6px rgba(0, 0, 0, 0.15);
    }
  </style>
  
        </form>
      </div>
      </section>
    </div>
    <style>
      .btn-scroll {
        background: linear-gradient(135deg, #FF6B6B, #5563DE); /* Gradient Effect */
        color: white;
        padding: 12px 20px; /* Reduced Padding */
        border: none;
        border-radius: 40px; /* Smooth Round Edges */
        font-size: 16px; /* Slightly Smaller Font */
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease-in-out;
        box-shadow: 0 4px 12px rgba(85, 99, 222, 0.3);
        display: inline-flex;
        align-items: center;
        gap: 8px;
        text-transform: uppercase;
        letter-spacing: 1px;
        position: relative;
        overflow: hidden;
      }
    
      .btn-scroll:hover {
        background: linear-gradient(135deg, #5563DE, #FF6B6B); /* Reverse Gradient */
        transform: translateY(-3px); /* Gentle Lift Effect */
        box-shadow: 0 6px 18px rgba(255, 107, 107, 0.4);
      }
    
      .btn-scroll:active {
        transform: scale(0.96); /* Smooth Click Effect */
        box-shadow: 0 2px 6px rgba(255, 107, 107, 0.3);
      }
    
      .btn-scroll::before {
        content: "";
        position: absolute;
        top: 50%;
        left: 50%;
        width: 300%;
        height: 300%;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 10%, transparent 70%);
        transition: all 0.6s ease-out;
        transform: translate(-50%, -50%) scale(0);
      }
    
      .btn-scroll:hover::before {
        transform: translate(-50%, -50%) scale(1);
        opacity: 0;
      }
    </style>
    
   <script>
      function scrollToSale() {
  document.getElementById('saleEntry').scrollIntoView({ behavior: 'smooth' });
}

    </script>
    <style>
      
      
      .card-headerr {
    background: rgba(255, 255, 255, 0.2); /* Light glass effect */
    backdrop-filter: blur(10px); /* Soft background blur */
    -webkit-backdrop-filter: blur(10px);
    padding: 25px 30px;
    text-align: center;
    font-family: 'Poppins', sans-serif;
    font-size: 2rem;
    color: #3346a0;
    border-bottom: 3px solid transparent;
    border-image: linear-gradient(to right, #5563DE, #74ABE2) 1;
    position: relative;
    border-radius: 15px 15px 0 0; /* Smooth rounded top corners */
    box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.1); /* Soft shadow */
    transition: all 0.4s ease-in-out;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
}

.card-headerr::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 50%;
    transform: translateX(-50%);
    width: 70px;
    height: 5px;
    background: linear-gradient(to right, #74ABE2, #5563DE);
    border-radius: 2px;
}

.card-headerr i {
    font-size: 2rem;
    color: #3346a0;
    transition: transform 0.3s ease-in-out;
}

.card-headerr:hover {
    background: rgba(255, 255, 255, 0.3);
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.15);
    transform: translateY(-3px); /* Slight lift effect */
}

.card-headerr:hover i {
    transform: rotate(10deg) scale(1.1); /* Subtle icon animation */
}


      .card-body {
        background: #fff;
        padding: 40px;
      }
      .form-label {
        font-weight: 700;
        color: #5563DE;
      }
      .form-control {
        border-radius: 10px;
        border: 1px solid #ced4da;
        box-shadow: none;
      }
      .form-control:focus {
        border-color: #74ABE2;
        box-shadow: 0 0 5px rgba(116,171,226,0.5);
      }
      .item {
        position: relative;
        padding: 20px;
        border: 1px solid #e0e0e0;
        border-radius: 10px;
        background: #f8f9fa;
        margin-bottom: 20px;
        transition: transform 0.3s, box-shadow 0.3s;
      }
      .item:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      }
      .delete-btn {
        position: absolute;
        top: 15px;
        right: 15px;
        background-color: #dc3545;
        color: white;
        border: none;
        border-radius: 50%;
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: background-color 0.3s;
      }
      .delete-btn:hover {
        background-color: #c82333;
      }
      #addItem {
        margin-bottom: 20px;
      }
      .btn-custom {
        font-weight: 700;
        border-radius: 50px;
        padding: 10px 20px;
      }
    </style>
    <div class="tablee">
                        
                        <section class="table__body">
                            <table>
                              <thead>
                               
                                    <tr>
                                        <th data-sort-type="numeric"> #<span class="icon-arrow">&UpArrow;</span> </th>
                                        <th data-sort-type="string"> Date/Time <span class="icon-arrow">&UpArrow;</span></th>
                                        <th data-sort-type="numeric"> Tasklist <span class="icon-arrow">&UpArrow;</span></th> 
                                        <th data-sort-type="numeric"> Comment <span class="icon-arrow">&UpArrow;</span></th>  
                                        <th data-sort-type="string"> By <span class="icon-arrow">&UpArrow;</span></th>                    
                                        <th data-sort-type="string"> Action <span class="icon-arrow">&UpArrow;</span></th> 
                                        
                                    </tr>
                                </thead>
                                <tbody>
                                    <% let serialNumberr = 1; %>
                                    <% alldaybook.reverse().forEach(item => { %>
                                        <tr>
                                            <td><%= serialNumberr++ %></td>
                                            <td class="date-column">
                                              <%= new Date(item.Dateandtimedaybook).toLocaleString('en-GB', { 
                                                    weekday: 'short', 
                                                    year: 'numeric', 
                                                    month: 'short', 
                                                    day: '2-digit', 
                                                    hour: '2-digit', 
                                                    minute: '2-digit', 
                                                    second: '2-digit', 
                                                    hour12: false 
                                              }).replace(',', '') %>
                                            </td>
                                            
                                            
                                            <td><%= item.daybookinandout  %></td>
                                            <td><%= item.comment  %></td>
                                            <td><%= item.maker  %></td>
                                            <td> <a href=""><i class="fa-solid fa-pen-to-square"></i></a></td>
                                        
                                           
                                        
                                          </tr>
                                          
                                      <%    });  %>
                                      </tbody>
                                    </table>
                                    <hr style="border: 1px solid #c9c8c8; margin: 30px 0;">
                        </section>
                        
                      </div>
                    <script>
                        const search = document.querySelector('.input-group input'),
                    table_rows = document.querySelectorAll('tbody tr'),
                    table_headings = document.querySelectorAll('thead th');
                
                // 1. Searching for specific data of HTML table
                search.addEventListener('input', searchTable);
                
                function searchTable() {
                    table_rows.forEach((row, i) => {
                        let table_data = row.textContent.toLowerCase(),
                            search_data = search.value.toLowerCase();
                
                        row.classList.toggle('hide', table_data.indexOf(search_data) < 0);
                        row.style.setProperty('--delay', i / 25 + 's');
                    })
                
                    document.querySelectorAll('tbody tr:not(.hide)').forEach((visible_row, i) => {
                        visible_row.style.backgroundColor = (i % 2 == 0) ? 'transparent' : '#0000000b';
                    });
                }
                
                // 2. Sorting | Ordering data of HTML table
                // Assuming you already have these variables defined
                // let table_headings = document.querySelectorAll('th');
                // let table_rows = document.querySelectorAll('tbody tr');
                
                table_headings.forEach((head, i) => {
                    let sort_asc = true;
                    head.onclick = () => {
                        table_headings.forEach(head => head.classList.remove('active'));
                        head.classList.add('active');
                
                        document.querySelectorAll('td').forEach(td => td.classList.remove('active'));
                        table_rows.forEach(row => {
                            row.querySelectorAll('td')[i].classList.add('active');
                        });
                
                        head.classList.toggle('asc', sort_asc);
                        sort_asc = head.classList.contains('asc') ? false : true;
                
                        sortTable(i, sort_asc, head.dataset.sortType);
                    };
                });
                
                function sortTable(column, sort_asc, sortType) {
                    [...table_rows].sort((a, b) => {
                        let first_row = a.querySelectorAll('td')[column].textContent.toLowerCase();
                        let second_row = b.querySelectorAll('td')[column].textContent.toLowerCase();
                
                        if (sortType === 'numeric') {
                            // Convert text to numbers for numerical sorting
                            first_row = parseFloat(first_row);
                            second_row = parseFloat(second_row);
                        }
                
                        return sort_asc ? (first_row > second_row ? 1 : -1) : (first_row > second_row ? -1 : 1);
                    })
                    .map(sorted_row => document.querySelector('tbody').appendChild(sorted_row));
                }
                function confirmDelete(postId) {
                    var confirmation = confirm("Are you sure you want to delete this pooja item?");
                    if (confirmation) {
                        // User clicked "OK", proceed with deletion
                        window.location.href = "/deletepoojaitem/" + postId;
                    } else {
                        // User clicked "Cancel", do nothing
                    }
                }
                    </script>
                    </div>
                    
                  </div>
               
                 
           </section>
  </div>
</main>
<script>
function getCurrentDateTime() {
          const now = new Date();
          const year = now.getFullYear();
          const month = (now.getMonth() + 1).toString().padStart(2, '0');
          const day = now.getDate().toString().padStart(2, '0');
          const hours = now.getHours().toString().padStart(2, '0');
          const minutes = now.getMinutes().toString().padStart(2, '0');
          const dateTimeString = `${year}-${month}-${day}T${hours}:${minutes}`;
          return dateTimeString;
        }
    
      
      
      
      
      
      
      
      
        document.getElementById('datetimee').value = getCurrentDateTime();
</script>
<style>
  .submit-button {
    background-color: #3498db; /* Blue */
    border: none;
    color: white;
    padding: 15px 32px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 10px 0;
    cursor: pointer;
    border-radius: 5px; /* Rounded corners */
    transition: background-color 0.3s ease; /* Smooth color transition on hover */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
   width: 240px;
   margin: 35px;
  }

  .submit-button:hover {
    background-color: #2980b9; /* Darker blue on hover */
  }

 
 .money-receipt-container {
    margin-bottom: 10px;
    border: 1px solid #ccc;
    padding: 10px;   
}

.advance-button {
    margin-left: 10px;
    padding: 5px 10px;
    background-color: #3f72e0;
    color: white;
    border: none;
    cursor: pointer;
    border-radius: 5px;
}

.product-detail h2 {
  flex: 1;
  margin: 5px;
  padding: 6px;
  border: 1px solid #bbbaba;
  border-radius: 5px;
}



.container2 {
    width: 740px;
    height: 600px;
    margin: 30px ; /* Set left and right margins to auto */
}
.container {
    width: 400px;
    height: 500px;
    margin: 30px ; /* Set left and right margins to auto */
}

.card {
  border-radius: 25px;
  box-shadow: -11px 11px 1px rgba(0, 0, 0, 0.3);
  border: 1px solid #000000; 
}


.card-head {
  position: relative;
  height: 510px;
   
 border: #000000;
  border-radius: 25px ;
}


.product-detail h2 {
  font-size: 14px;
  padding-bottom: 10px;
  text-transform: uppercase;
}

.product-detail {
  padding: 10px 20px;
  font-size: 6px;
  color: #000000;
}




















</style>


<style>
  .tablee__header {
    width: 100%;
    height: 10%;
    background-color: #fff4;
    padding: .8rem 1rem;

    display: flex;
    justify-content: space-between;
    align-items: center;
}

.tablee__header .input-group {
    width: 35%;
    height: 100%;
    background-color: #fff5;
    padding: 0 .8rem;
    border-radius: 2rem;

    display: flex;
    justify-content: center;
    align-items: center;

    transition: 0.6s;
}

.tablee__header .input-group:hover {
    width: 45%;
    background-color: #fff8;
    box-shadow: 0 .1rem .4rem #0002;
}

.tablee__header .input-group img {
    width: 1.2rem;
    height: 1.2rem;
}

.tablee__header .input-group input {
    width: 100%;
    padding: 0 .5rem 0 .3rem;
    background-color: transparent;
    border: none;
    outline: none;
}
</style>










<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
clude('nav bar/navbardown.ejs') %>