<%- include('nav bar/navbar.ejs') %>
    <title>ITEM Database</title>
    <link rel='stylesheet' href='/stylesheets/productallstyle.css' />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>


<main class="table">
  <section class="table__header" style="color: #000000; font-size: 24px;">
    <h1 style="font-weight: bold;">Add Product</h1>
</section>   
        <section class="table__body">
           <table>
            <div class="form-container" style="height: 80vh; overflow-y: auto;">
          <form action="/editdatereceipt/<%= productEdit._id %>" method="post" id="contact_form">
                   

            <div class="flex flex-wrap p-6 rounded-md">
                <div class="w-full sm:w-1/2 md:w-1/3 lg:w-1/3 xl:w-1/3 px-4 mb-4">
                  <label class="block text-sm font-semibold text-black mb-2">Receipt date</label>
                  <div class="flex items-center bg-white rounded-md p-2 border border-gray-800">
                    <span class="text-teal-500"><i class="fas fa-calendar-alt"></i></span>
                    <input name="receiptDate" placeholder="Receipt Date" type="datetime-local" value="<%= productEdit.receiptdate %>" class="ml-2 w-full py-1 px-2 bg-transparent border-none focus:outline-none text-gray-800 placeholder-gray-500">
                  </div>
                </div>
              </div>
             <div>
                <%= productEdit.receiptdate %>
             </div>
          
<script>
    function formatDate(dateString) {
      if (!dateString) return ""; // Return empty string if date is not provided
  
      const dateObject = new Date(dateString);
      const year = dateObject.getFullYear();
      const month = String(dateObject.getMonth() + 1).padStart(2, "0");
      const day = String(dateObject.getDate()).padStart(2, "0");
      const hours = String(dateObject.getHours()).padStart(2, "0");
    const minutes = String(dateObject.getMinutes()).padStart(2, "0");
    const seconds = String(dateObject.getSeconds()).padStart(2, "0");


      return `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`;
    }
  
    // Usage
    const formattedDate = formatDate("<%= productEdit.receiptdate %>");
    console.log(formattedDate); // Check if the formatted date is correct
  
    // Set the formatted date as the value of the input field
    document.querySelector('input[name="receiptDate"]').value = formattedDate;
  </script>
  
              

              
      
             

             
       
            
      
         
          
      
         
          
      
        
         
        
        <div class="p-4 flex justify-center items-center">
            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                Update Receipt Date<span class="ml-2">🚀</span>
            </button>
        </div>
        
             
          </form>
          </div>
          </table>
            </section>
            
        </main>
        <script src="./javascripts/productall.js"></script>
        <%- include('nav bar/navbardown.ejs') %>
    